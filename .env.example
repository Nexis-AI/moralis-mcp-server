# Required
MORALIS_API_KEY=YOUR_MORALIS_API_KEY

# Optional (defaults shown)
MCP_TRANSPORT=stdio
PORT=3000
SERVER_NAME=Moralis MCP
SERVER_VERSION=

# Optional: choose a single API schema (otherwise both EVM + Solana are enabled)
# SERVER_CONFIG=evm
# SERVER_CONFIG=solana
